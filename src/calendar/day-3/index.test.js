const { expect } = require("chai");

const { p1, p2 } = require(".");

const input = [];

describe("Day2", () => {
  describe("Part One", () => {
    it("", () => {
      const result = p1(input);
      expect(result).eql(0);
    });
  });

  describe("Part Two", () => {
    it("", () => {
      const result = p2(input);
      expect(result).to.eql(0);
    });
  });
});
